# TODO

- [x] 현재 `app.py`의 지수 데이터 처리 흐름과 카드 렌더링 위치를 확인한다.
- [x] KOSPI/KOSDAQ 각각에 대해 최근 30분(장 시작 후 30분 이내면 장 시작부터 현재까지) 기준 추세 계산 로직을 구현한다.
- [x] 상승/하락/보합 상태를 직관적으로 보여주는 반짝이는 추세 카드를 추가한다.
- [x] 검증을 실행하고 결과를 기록한다.

# Review

- 최근 체결 시각 기준으로 30분 전과 장 시작 시각(09:00) 중 더 늦은 시점을 추세 판단 시작점으로 사용하도록 구현했다.
- KOSPI, KOSDAQ 각각에 대해 상승/하락/보합을 배경색과 반짝이는 하이라이트 애니메이션으로 구분하는 카드 UI를 추가했다.
- 검증: `python3 -m py_compile /Users/hyunsikhwang/dailystock/app.py` 통과
- 테스트: 미실행 (사유: `make test`, `npm test`, `pnpm test`, `yarn test`, `pytest -q`에 해당하는 표준 테스트 구성이 저장소에 없음)
